language: swift # or objective-c
osx_image: xcode9
script:
  - xcodebuild -scheme Jeu de Nim -workspace Jeu de Nim.xcodeproj -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone SE,OS=11' build test
after_success:
  - bash <(curl -s https://codecov.io/bash)
